# Database Configuration
# PostgreSQL connection string (required for database operations)
# Example: postgresql://user:password@localhost:5432/database_name
DATABASE_URL=

# Server Configuration
# Port for the application server (default: 5000)
# Note: In Replit, this is automatically set by the platform
PORT=5000

# Session Secret
# Secret key for session encryption (required for secure sessions)
# Generate a secure random string for production use
# Example: openssl rand -base64 32
SESSION_SECRET=

# Email Configuration (Optional - for production)
# Uncomment and configure when ready to enable email notifications
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=noreply@questlegal.co.kr

# Environment
# Set to 'production' when deploying to production
NODE_ENV=development
